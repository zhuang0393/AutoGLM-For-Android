<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="280dp"
    android:layout_height="wrap_content"
    android:background="@color/dialog_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Icon -->
        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_touch"
            android:tint="@color/status_waiting"
            android:contentDescription="Manual Action" />

        <!-- Title -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:text="@string/takeover_title"
            android:textColor="@color/status_waiting"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium" />

        <!-- Message -->
        <TextView
            android:id="@+id/tv_takeover_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="@string/takeover_message"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

        <!-- Instructions -->
        <TextView
            android:id="@+id/tv_takeover_instructions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/takeover_instructions_background"
            android:padding="12dp"
            android:text="@string/takeover_instructions_placeholder"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:lineSpacingMultiplier="1.2" />

        <!-- Continue button -->
        <Button
            android:id="@+id/btn_continue"
            style="@style/Widget.App.Button.Tonal"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="16dp"
            android:text="@string/takeover_continue"
            android:drawableStart="@drawable/ic_check"
            android:drawablePadding="8dp"
            android:gravity="center_vertical|start" />
    </LinearLayout>
</FrameLayout>
